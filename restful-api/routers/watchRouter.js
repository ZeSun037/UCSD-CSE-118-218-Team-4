const express = require('express');

const watchRouter = express();

watchRouter.get('/:id', (res, req, next) => {

})

watchRouter.delete('/:id/:taskid', (res, req, next) => {

})

module.exports = watchRouter;